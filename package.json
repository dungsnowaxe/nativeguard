{
  "name": "nativeguard",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.10.0",
  "scripts": {
    "build": "tsc -b packages/schema packages/rules packages/core packages/cli packages/github-action",
    "check": "tsc -b packages/schema packages/rules packages/core packages/cli packages/github-action",
    "test": "pnpm -r --if-present test",
    "validate:rules": "pnpm --filter @nativeguard/rules validate",
    "nativeguard": "node packages/cli/dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.10",
    "typescript": "7.0.2"
  }
}
